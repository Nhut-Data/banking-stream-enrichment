[project]
name = "banking-stream-enrichment"
version = "0.1.0"
description = "Real-time transaction enrichment pipeline using Berka dataset"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
